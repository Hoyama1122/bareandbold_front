# UI Components Organization

- `/ui`: Basic atomic elements (Buttons, Inputs, Modals, Cards, Dropdowns).
- `/storefront`: Customer-facing UI components (Navbar, Footer, ProductListing, CartDrawer).
- `/backoffice`: Vendor & Employee administration dashboard UI components (Sidebar, StatsCard, ProductTable, OrderDetail).
